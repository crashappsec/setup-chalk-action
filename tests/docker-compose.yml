services:
  action:
    build:
      context: .
      args:
        BASE: ${BASE:-alpine}
    working_dir: /action
    user: ${USER:-root}
    environment:
      CHALK_PASSWORD: ${CHALK_PASSWORD:-}
    volumes:
      - ../:/action
      - ../../chalk:/chalk
